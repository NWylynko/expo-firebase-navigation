# expo-firebase-navigation
very simple example of how to setup firebase and navigation in expo

## to run / test out

1. install node
2. `npm i -g expo-cli`
3. clone and cd into repo
4. `expo install`
5. `expo start`
